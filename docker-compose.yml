services:
  mysql:
    image: mysql:8.0
    container_name: attendance-manager-mysql
    restart: unless-stopped
    ports:
      - "3307:3306"
    environment:
      MYSQL_ROOT_PASSWORD: password
      MYSQL_DATABASE: attendance_manager
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data:
